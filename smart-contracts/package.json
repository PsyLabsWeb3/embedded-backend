{
  "name": "embedded-smart-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:base-sepolia": "hardhat run scripts/deploy.cjs --network baseSepolia",
    "deploy:base": "hardhat run scripts/deploy.cjs --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
